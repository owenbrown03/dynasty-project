services:
  worker:
    scale: 1
    command: >
      python -m debugpy
      --listen 0.0.0.0:5678
      --wait-for-client
      -m taskiq worker
      --no-configure-logging
      app.core.worker:broker
    ports:
      - "5679:5678"