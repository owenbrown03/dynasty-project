{
    "name": "Orphan Finder Project",
    "dockerComposeFile": "../docker-compose.yml",
    "service": "api", // Must match your YML service name
    "workspaceFolder": "/app", // Must match the 'target' in your YML volumes
    "postCreateCommand": "",
    "shutdownAction": "stopCompose",
    "customizations": {
        "vscode": {
            "extensions": ["ms-python.python", "ms-python.vscode-pylance"]
        }
    }
}